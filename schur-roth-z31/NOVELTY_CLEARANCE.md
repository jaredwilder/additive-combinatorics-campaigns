# Novelty clearance — Schur–Roth subsets of `Z/31Z`

## Claim searched

The exact maximum and classification of subsets `A ⊆ Z/31Z` that are simultaneously:

- standard sum-free: `(A+A) ∩ A = ∅`;
- nontrivial three-term-progression-free.

The result searched was:

- maximum cardinality `6`;
- exactly `330` extremal six-sets;
- exactly `12` dilation orbits under `(Z/31Z)^×`.

## Search families used in the recovered packet

The source clearance used exact and broadened searches including:

- `"sum-free and 3-AP-free"`;
- `"simultaneously sum-free" progression-free`;
- `"sum-free" "3-term arithmetic progression" cyclic group`;
- `"Z/31Z" sum-free progression-free`;
- arXiv-restricted variants;
- OEIS searches for combined sum-free/progression-free terminology and the computed finite sequence;
- nearest-neighbor literature on `W`-avoiding sets, complete 3-AP-free sets, ordinary sum-free sets, and tri-colored sum-free sets.

The closest sources found concerned complete/saturating 3-AP-free sets, ordinary sum-free sets,
`(k,l)`-sum-free sets, tri-colored sum-free sets, or avoidance of one or multiple linear equations.
The search did not locate the exact simultaneous `Z/31Z` problem, the value 6, the count 330, or
the 12-orbit classification.

## Verdict boundary

```text
mathematical truth: PROVED BY EXACT EXHAUSTIVE ENUMERATION
novelty status: APPARENTLY NEW AFTER SYSTEMATIC SEARCH
safe historical wording: "to the best of our knowledge"
```

This is not a priority certificate or peer-review substitute. Novelty status is not a premise in
the mathematical certificate.
